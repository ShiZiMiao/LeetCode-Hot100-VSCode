# Changelog

All notable changes to the "leetcode" extension will be documented in this file.

## 0.1.15

- **修复本地调试链表/树题全部运行出错**：Python 驱动的 ListNode/TreeNode 入参转换是死代码（解法函数直接吃到裸 list，链表/树题一律报 `'list' object has no attribute 'val'`，远程提交却正常）——现按签名注解逐参数转换（含 `List[ListNode]` 序列形态；标量给出的节点引用降级单节点防崩溃），空链表/空树返回 `None` 按判题序列化为 `[]`（第 21 题示例 2 `[[],[]]` 此前误报不通过）；环形链表题（141/142）的 `pos` 尾接成环，结果序列化对环安全截断（141 此前误报失败，142 有死循环风险）

- **本地调试 Hot 100 全量审计修复**：以官方真实用例/期望输出把 100 题全部过一遍本地调试链路后修复审计发现的问题——① 用例与示例期望改为**按值配对**（官方 `exampleTestcases` 可能含展示示例之外的附加用例且位置不定：94 的隐藏用例插在中间，此前按位置配对会期望错位、真实解法被误报不通过；附加用例显示"未校验"，输入提取不全时自动退回位置配对）；② 236 LCA 节点引用题：`p`/`q` 以节点值给出时按值在结构内查找真实节点，结果按节点值比对（此前结果比对与引用语义都不还原）；③ 空结构原地修改题（114 空树）按判题序列化 `[]` 比对（此前示例 2 误报不通过）；④ "未校验"不再汇总成"❌ 有失败"（142 等非标准期望文本题此前看起来像挂了）；⑤ Python 风格单引号示例输入（79/200 题面形态）可正常提取作配对锚点。全量审计 100/100 通过（91 全过 + 6 特殊题正确跳过 + 3 含未校验用例），20 题另用真实解法验证数值判分；单测 110 全绿（新增输入提取/按值配对/空树原地修改等 8 个用例）

- **发版前全面审查修复（64 项）**：按代码规范/可读性/耦合/死代码/功能正确性/安全工程全量审查后修复——安全面：独立「查看题解」支线整体废弃（无 CSP + 原始 HTML 直插 + slug/summary 未转义的注入面，改为并入题面面板题解页统一管线，顺带消除 ~270 行双实现）；webview CSP 收紧（script-src 改 nonce + 收窄到 KaTeX 单域、connect-src 收窄到 leetcode.cn/阿里云）并移除全部内联 onclick（统一 click 委托）；题面原始 HTML 增加消毒（剥 script/iframe/事件属性/伪协议）；SSRF 防护修复 IPv6 永假分支（URL.hostname 保留方括号的根因）并增加解析后 IP 复核（防 DNS rebinding）；`escapeHtml` 补单引号转义；highlight.js 执行前 sha256 供应链校验；视频缓存文件名白名单。功能面：快速运行路径切分改 `path.dirname/basename`（修复 macOS/Linux 完全不可用）；语言 slug 归一收口（Go 的 `golang` 别名不再落 `.txt`）；动画帧正则修复旧格式匹配与 URL 二次转义坏址；树状态加载并发去重 + 世代号防过期回写；判题轮询遇瞬时网络故障不再放弃；二进制下载重定向加上限；openProblem 空数据不再静默；面板关闭后晚到渲染不再抛 disposed；判题响应类型化（`any` → `JudgeCheck` 最小接口）；判题发起失败提示摘要化；测试/提交流程合并为 `runJudgeFlow`。工程面：`playwright-core` 钉死 1.63.0 + `packageManager`/`engines.node` 声明；`.vscodeignore` 补 `out/test/**` 等（脚手架产物不再进商店包）；CI 权限收窄 `contents: read`、pnpm 版本统一、vsce 打包器钉版；29 段重构遗留孤儿注释与 13 处过度导出清理。回归：compile/lint 全绿、单测 110 全绿、Hot 100 驱动全量审计复跑 100/100

## 0.1.14

- **每日一题改为 Hot 100 随机**：不再拉官网今日题目（可能不在 Hot 100 内），改为按日期从 Hot 100 确定性随机抽取——每天固定一题、跨天轮换；无需登录与网络，侧栏节点直接显示当天题目
- **跨设备代码同步**：提交历史可把云端提交的代码恢复回本地 `leetcode/{题号}_{slug}.{ext}`（覆盖/另存副本可选，语言按提交时为准）；打开题目时若本地无代码文件而云端有历史提交，自动询问是否恢复——A 电脑提交、B 电脑登录同账号即可找回代码
- **视频题解增强**：自绘控制栏新增倍速下拉菜单（0.5×/0.75×/1.0×/1.25×/1.5×/2×，当前项高亮、点外部/Esc 关闭，主流播放器交互）——画面走 `<video>` 原生 `preservesPitch`、音频经 SoundTouchJS 保调变速（vendor 0.3.0，音色不随倍速改变，1× 仍走原生零依赖路径）；视频下载链路（VOD 播发地址/m3u8/HLS 分段）补瞬断重试（此前 TLS 握手被切一次即整段失败，与主判题链路同等兜底）
- **webview 安全加固**：题面/题解/登录面板补 Content-Security-Policy（默认零信任，仅放行扩展资源/内联交互/https CDN）；文章外链图片/视频下载补安全校验（仅 https、拒绝私网/回环地址）与 32MB 单次上限
- **非 Hot 100 题目入口收口**：错题回顾队列不再接收非 Hot 100 题（历史遗留条目启动时自动清洗），打卡/今日目标统计仅计 Hot 100 题；题面/判题/调试/题解等通用流程对外部文件题保持可用
- **修复错题提交风险**：文件名指向另一道题而题面拉取失败时，不再静默拿上一题的 currentProblem 顶替（网络瞬断会把当前文件代码提交到错误的题目）——统一提示并中止，测试/提交/调试/提交历史/题解等全部入口一致
- **特殊判题题自适应识别**：本地调试驱动不再按逐题 slug 名单判定（新题/非 Hot 100 题漏判）——设计类题（无 class/impl Solution：LRU、最小栈、前缀树、数据流中位数等）按代码结构识别（Python/Java/C++/Rust），JS/TS 按用例形状（操作序列）运行期识别，138（Node 带 random）/160 保留兜底；设计类题的 Java/C++ 调试模板不再因带参构造编译失败，Python 驱动加载期不再断言 Solution 存在
- **修复 when 子句语法**：编辑器标题栏按钮的 `resourcePath !~ /notes/` 不是合法运算符（新版 VS Code 严格解析报「意外的令牌」，F5 启动失败），改为 `!(resourcePath =~ /notes/)`
- **工程**：每日一题抽题、特殊题识别入 utils 并补单测（驱动级用例：设计类不崩溃/操作序列兜底/常规题回归）；`.gitignore`/`.vscodeignore` 排除仓库根 `leetcode/` 练习目录（避免打进 VSIX）；README/MARKETPLACE/AGENTS 同步

## 0.1.13

- **侧栏入口补全**：新增「⭐ 我的收藏」分组（有收藏才显示，子项按当前分组顺序排列，右键题目行取消收藏即时反映）；「📅 今日每日一题」一键节点（点击直达，不再只藏在命令面板）
- **复习错题流程**：命令「LeetCode: 复习错题」按到期顺序逐题打开（到期的优先），今日已复习记录跨会话保存（按日期自动重置），全部复习完提示「今日错题已全部复习完成 🎉」
- **快捷键补齐**：每日一题 `Ctrl+Alt+D`、随机抽题 `Ctrl+Alt+R`、复习错题 `Ctrl+Alt+W`、打开笔记 `Ctrl+Alt+N`、收藏/取消收藏 `Ctrl+Alt+L`（macOS 为 Cmd+Alt）
- **修复随机抽题按钮空白图标**：`$(shuffle)` 不是合法 codicon（VS Code 官方图标清单无此名，渲染为空白），改为 `$(sparkle)`；发版前图标名已对照官方 icons-in-labels 清单核查
- **修复提交历史 URL 边界**：submissionList 的 url 为空或不存在时可能拼出 `https://leetcode.cn/` 首页（而非该次提交页），现统一回退为「提交 id + slug」构造的完整地址
- **修复本地调试驱动用例嵌入 SyntaxError**：样例为带引号的字符串（如第 20 题括号题，行尾以 `"` 结尾）时，Python 驱动把用例直接拼进 `"""..."""` 会与闭合定界符连成 4 连引号报 `unterminated string literal`——用例改为 base64 内嵌（与期望输出同一机制）；JS/TS 驱动同步改为 JSON 转义内嵌（防反引号/`${}`/换行破坏）
- **修复 webview HTML 注入与转义缺失**：社区题解标题/作者、题面标题等 API 文本直接拼入 HTML 会破坏布局（标题含 `<` 等特殊字符时页面错乱）；独立「查看题解」面板的文章全文未经渲染器直接输出（原始 Markdown/HTML 注入的风险点）——全部改走 `escapeHtml`/`renderMarkdownToHtml`
- **修复图片本地化前缀包含损坏**：某图 URL 是另一 URL 前缀时（同图带/不带查询参数），短 URL 先替换会把长 URL 切断——改为按 URL 长度降序替换
- **修复快速运行在 PowerShell 下失效**：Java/C++/Rust/C 的编译运行命令用 `&&` 拼接，Windows 默认 PowerShell 5.1 不支持——改为逐条 sendText（先 cd 再编译再运行，cmd/PowerShell/bash 通吃）
- **工程**：`pickReviewCandidate` 纯逻辑（到期优先/跳过已复习/统计）入 `utils/wrongQueue.ts` 并补单测；AGENTS.md/README/MARKETPLACE/ROADMAP 同步

## 0.1.12

- **侧栏按难度分组**：分组方式可在「按分类（官网默认）/按难度」间切换（侧栏标题按钮或命令面板，选择持久化）——难度分组按 简单 → 中等 → 困难 排列，同一难度内按题号从小到大；难度数据随静态数据离线可用，接口实时值优先
- **搜索与抽题**：题目搜索（题号/中文名/英文名/分类模糊检索，回车直达，`Ctrl+Alt+F`）；随机抽题（全部/未做优先/简单/中等/困难/随机错题六种模式）；每日一题（官网 `todayRecord` 接口，可能不在 Hot 100 内会注明）
- **刷题统计节点**：侧栏「📈 刷题统计」——总进度、难度/分类分布、连续打卡天数、今日目标（默认 3 题/天，可点击修改或改设置 `leetcode.dailyGoal`）；打卡日期按提交通过本地累计
- **错题回顾增强**：失败次数累计、间隔复习排期（1/3/7/14/30 天到期待复习，到期的排最前）、随机错题抽检
- **提交历史**：查看当前题最近 20 条提交（状态/语言/时间/耗时内存），点击在浏览器打开对应提交页
- **下一题与快捷键**：按当前分组顺序跳下一题（`Ctrl+Alt+→`）；新增默认快捷键：测试 `Ctrl+Alt+T`、提交 `Ctrl+Alt+S`、搜索 `Ctrl+Alt+F`（macOS 为 `Cmd+Alt`）
- **列表元数据**：资料行展示通过率（修正为百分比小数换算，与官网一致）、会员题 🔒 标记；本地收藏 ⭐（右键题目行或命令面板切换，离线可用）
- **题目笔记**：`leetcode/notes/{题号}_{slug}.md`（思路/复杂度/备注模板），题目行右键或命令面板打开
- **相似题目推荐**：题面页底部「🧩 相似题目」，点击直达（相似题可能不在 Hot 100，题面/判题/题解流程通用）
- **多根工作区支持**：打开题目/本地调试/笔记自动定位到当前正在做的工作区（不再固定写第一个）
- **修复题解刷新卡死**：VS Code webview 对内容完全相同的 `panel.webview.html` 赋值不会触发导航，刷新重建的页面与首次加载字节级一致导致停在「加载题解中」——页面生成注入唯一 nonce 保证任何赋值必然导航；刷新改为缓存先行（后台重取、内容一致时跳过二次赋值），首次加载整体 60s 超时给出明确的错误页
- **修复官方动画平铺**：LeetCode 官方文章动画帧格式已改为 `<![fig1](url),...,![figN](url)>`（alt 从毫秒数变为 figN），播放器转换正则只认旧格式导致所有帧平铺——现在两种格式兼容（非数字 alt 取默认 1200ms），帧图全部本地化（离线可播放）
- **修复每日一题接口失效**：`questionOfToday` 查询已被 leetcode.cn 移除（GraphQL 校验 400），迁移为前端最新使用的 `todayRecord` 查询并从官方前端 chunks 核实字段
- **修复提交历史打开报错**：`submissionList` 的 url 为相对路径，`openExternal` 误当本地文件执行（0x2；找不到文件）——统一补全为绝对 `https://leetcode.cn/...` 地址并校验协议
- **工程**：题解加载打点诊断（每步耗时入扩展控制台）；纯逻辑新增 difficultyGroups/studyStats/questionOrder/metaFormat/similarQuestions 模块及 23 个单测（共 80 个）；AGENTS.md/README/MARKETPLACE 同步

## 0.1.11

- **修复 .py 误以 Python 2 提交**：leetcode.cn 的 `python` 语言是 Python 2 判题环境（注解语法直接 SyntaxError），此前部分流程会把 .py 文件按 Python 2 提交——现象是本地调试通过、远程测试/提交报 `def ...` 行语法错误（如第 15 题）。现在 .py 统一按 **python3** 提交；语言选择器移除 Python 2 条目（二者同为 .py 扩展名，极易误选）；此前被写入的错误语言状态会在测试/提交时按当前文件扩展名自动纠正
- **修复官方题解多语言代码块丢失**：C++ 模板代码（`vector<vector<int>>`）与注释里的 `b<c` 会被题解渲染前处理的标签清理正则误删/误换，连带 Python/Java/Go 的代码块与围栏行被删除，多语言标签页不显示（第 15 题三数之和等）。现在代码围栏与行内代码不再参与 HTML 标签清理，第 15 题题解恢复 4 语言标签页（Python 优先默认选中）
- **修复语言映射不全**：文件身份解析按扩展名补齐 C#/Kotlin/Swift/Ruby/Scala/PHP，避免这些语言的文件以其他语言提交
- **修复侧栏视图图标缺失**：`resources/leetcode.svg` 曾被排除出 VSIX（打包清单遗漏），安装版树视图图标回退为默认图标
- **工程**：删除无引用的 `getLanguageInfo` 死导出；AGENTS.md 架构文档同步（移除已删除的旧死代码条目）

## 0.1.10

- **题解公式修复**：LaTeX 数学公式不再显示为源码、乘号不再丢失——修复了 Markdown 斜体规则与公式内星号（`*`）的冲突，公式 `$...$`/`$$...$$` 现在正常渲染（KaTeX），即使公式源文本也完整保留
- **官方动画演示**：官方题解里的帧序列动画渲染为与网页版一致的播放器——点击播放自动轮播（可暂停），支持上一帧/下一帧与 `1 / 8` 页码指示
- **题面与题解还原官网**：题目页新增话题标签（数组、双指针等）；题解文章头部显示作者头像与昵称、👑 官方徽章、发布日期与点赞数（与官网文章页一致）
- **视频题解重做**：自绘控制栏（进度条可点击/拖动、时间显示、音量滑杆与静音、全屏铺满面板、点击画面播放/暂停、2.5 秒无操作自动隐藏）；**修复 webview 无声问题**——VS Code 的 webview 不支持 AAC 音频与自动播放（平台限制），现改为 MP4 仅画面 + MP3 音频经 WebAudio 独立解码播放，声音稳定输出、音画同步（进度跳转/暂停均跟随）
- **进度与复盘**：侧栏首行显示总进度（已解 n / 100 + 百分比），每个分组显示「已解 x / 总数」；状态筛选（全部/未做/尝试过/已解决）——点击总进度行或命令面板「状态筛选」切换，判题结果实时更新侧栏与分组进度
- **错题回顾队列**：提交失败的题目自动记录（失败时间/原因），侧栏「❌ 错题回顾」置顶展示、点击重开题目；提交通过自动移出，支持一键清空
- **工程**：`extension.ts` 模块化拆分（题面/题解面板生成与渲染器、判题上报、webview 消息协议类型化），删除历史死代码；新增 `pnpm release <version>` 发布脚本（版本/CHANGELOG 校验、打包、tag、GitHub Release、Open VSX 一条龙并输出商店上传指引）；打包清单加固（排除 .zcode/.github/构建脚本等不应进包的目录）；单元测试增至 57 个

## 0.1.9

- **一键登录**：登录面板新增「打开浏览器自动登录」——用系统默认浏览器（Edge/Chrome）打开真实的 LeetCode 登录窗口（全新隔离临时配置，用后即删，插件不经手账号密码），完成登录（密码/验证码/扫码/第三方均可）后自动读取会话并校验存储；默认浏览器不可自动接管时回退已装的 Edge/Chrome 并提示。手动粘贴 Cookie 保留为兜底，并支持**整段 Cookie 粘贴自动拆分**（不用再分别抠两个值）
- **按用例展示判题结果**：运行测试的判题详情按用例分组输出（用例 N：通过状态 / 输入 / 输出 / 预期结果），不再把全部输入、输出、预期分三段罗列；提交判题保留汇总视图
- **本地调试对齐判题机**：原地修改、无返回值的题（移动零、旋转图像、合并有序数组等）正确比对**被修改后的入参**而非返回值 `None`；调试会话中驱动文件对调试器完全不可见——断点只在你的题解代码里生效，单步/步出穿过驱动、调用栈隐藏驱动帧
- **判题过程更稳**：测试/提交在途忙碌锁（快速连点不再并发判题/重复提交）；判题轮询退避延长至 ~90 秒并显示状态栏等待秒数；提交超时 toast 一键打开官方提交页
- **会话过期主动提示**：启动时后台校验 + 请求中实时检测登录态失效，及时提示重新登录（不再表现为各种难懂的失败）
- **安全**：恢复 TLS 证书严格校验（移除历史降级开关；网络瞬断的超时/禁复用/幂等安全重试保持不变）
- **商店**：分类改为 Education / Programming Languages；商店详情页内容对齐精简后的 README
- **工程**：移除死依赖 `marked`；核心纯逻辑（判题报告/题面解析/Cookie 组装/重试判定）抽为独立模块并补 41 个单元测试，新增 CI（lint + 编译 + 单测）

## 0.1.8

- **更名与换标**：显示名称改为 **LeetCode Hot100 Pro**，新图标（琥珀渐变 + 火焰 + 100），与上游 fork 原插件正式区分（扩展 ID `ShiZiMiao.leetcode` 不变，自动更新不受影响）
- **判题详情通道**：每次测试/提交（无论通过与否）的完整判题信息写入输出面板「LeetCode 判题结果」——状态（中英双语）、通过用例数、首个失败用例、输入/输出/预期结果、编译/运行时错误，图标标识（✅/❌/🧪/🚀/📥/📤/🎯 等），判题后自动展开面板；右下角 toast 只留一行简讯，附「原始判题响应」「在浏览器打开」按钮
- **原始判题响应**：可经 toast 按钮或命令「LeetCode: 查看最近一次原始判题响应」在 JSON 编辑器中查看（支持折叠）
- **每题单页**：题面页/题解页按题去重，重复点击只聚焦已打开页面（含快速双击防竞态）；「查看题解」编辑器标题按钮移除，命令面板仍可用
- **修复误提交**：焦点在输出面板等非代码视图时，测试/提交/本地调试不再把面板内容当代码提交，自动回退到当前题解文件
- **网络稳定性**：TLS 偶发瞬断自动安全重试（GET 总重试；POST 仅在请求体未完整发出前重试，杜绝重复提交），失败提示改为中文
- **调试体验**：缺少 Python 调试扩展时可一键安装 ms-python.debugpy；本地调试不支持语言的提示更新为实际支持列表（Python3/Java/C++/JS/TS/Go/Rust）
- **商店信息规范**：商店详情页不再展示 fork 说明（fork 归属移至仓库 README 文末一行）；README 全面对齐当前实现（版本要求、SecretStorage 存储、渲染管线、双商店安装入口、调试文件命名与语言支持）

## 0.1.7

- **本地调试环境与判题机对齐**：按 LeetCode 判题机 `globals()` 实测重建驱动预导入——星导入 `string/re/collections/heapq/bisect/copy/math/random/statistics/itertools/functools/operator/io/sys/json`（含 Counter/defaultdict/lru_cache/inf 等散名），`time/os` 仅模块名，`datetime` 模块 + 常用类，`typing` 最后导入；`sortedcontainers` 本地未安装时自动以 bisect 兜底；补齐 LC 定制的 `heappush_max` 系列大顶堆函数；修复解法中直接引用 `collections.defaultdict` 等模块名时报 NameError 的问题（官方有而本地此前未注入）；移除官方不存在的 `queue`

## 0.1.6

- **修复错题状态残留**：运行/调试/测试/提交/查看题解此前以全局记录的"当前题"为准，打开新题但流程未完整走完（如语言选择被取消、窗口重启）时会用到上一题状态，导致运行/提交错题。现全部改为**以实际编辑的文件为准**（文件名 `{题号}_{slug}.{ext}`），状态不一致时自动按文件重新拉取题面并同步
- **本地调试比对修正**：期望输出比对与 LeetCode 判题语义一致——tuple 与 list 等价（如两数之和返回 `(0, 1)` 通过）、int/float 数值等价；bool 与 `1` 不再误判通过
- **C++ 编译错误行号**：`main.cpp:3:5: error:` 格式可正确解析定位
- **打开题目优化**：题目已有代码文件时不再弹语言选择框，直接打开

## 0.1.5

- **本地调试增强**：Python 一键启动 VS Code 原生调试会话（importlib 驱动，断点直接打在题解代码上）；自动提取题面各示例的期望输出并逐例比对；调试文件统一放入 `debug/` 子目录
- **运行/提交错误诊断化**：错误不再弹长文本，而是标注到编辑器（波浪线 + 问题面板），重跑自动清除旧标注，可一键跳转问题面板
- **网络稳定性**：请求 20s 超时兜底 + 禁用 keep-alive 复用（代理环境避免死连接无限挂起）；题解加载改为官方/社区并行拉取
- **题解页刷新按钮**：题目描述、题解两个标签可独立刷新，加载失败时无需重开面板

## 0.1.4

- **题目难度显示**：题目行现在显示难度，与题号一起放在中括号内（如 `[1 · 简单]`），数据实时取自 leetcode.cn
- **修复状态/难度缺失**：列表接口单页最多返回 100 题，原实现 limit 3000 被截断，导致 128 号以后的题目取不到状态与难度——已改为按 skip 分页并行拉取，Hot 100 全部 100 题的状态图标与难度均能正确显示
- **数据对齐官网**：补充缺失的 152. 乘积最大子数组（Maximum Product Subarray），题目列表与官网《热题 100》完全一致
- **发布元信息完善**：description、license（MIT）、homepage、bugs 字段；README 克隆地址修正为当前仓库

## 0.1.3

- 题目列表状态图标匹配 leetcode.cn 大写状态枚举（AC/TRIED/NOT_STARTED）：已通过显示绿色对勾，尝试过未通过显示红斜杠；提交通过后自动刷新列表状态
- 补充 publisher 字段（ShiZiMiao），扩展 ID 更正为 `ShiZiMiao.leetcode`

## 0.1.2

- 官方题解代码正常显示（byLeetcode 官方文章优先，含真实多语言代码与标签页）
- 视频题解完整链路（playAuth → HLS 合并 → ffmpeg remux 为 MP4 → 原生播放，自动缓存）
- 题解渲染加固（完整 Markdown 渲染、CRLF 兼容）、代码高亮（highlight.js 本地打包 + GitHub 明暗双色板）、代码块复制按钮
- 图片本地化、主题适配（深浅主题下清晰可读）等累计修复

## 0.1.1

- 修复题解 iframe 登录页重定向问题（`sanitizeSolutionContent` 移除跨域 iframe）